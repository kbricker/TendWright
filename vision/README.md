# vision/

P2 — Vision-guided picking (sim camera, then webcam + AprilTags + hand-eye
calibration), plan #606. P3 — Quality inspection ML (synthetic training data
from sim, validate on real photos), plan #607.
