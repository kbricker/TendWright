"""Bench bring-up CLI tools for the SO-101 arm and the bin camera.

Every tool runs as `uv run python -m hardware.bench.<tool>` and must fail
with a clear one-line error (not a traceback) when hardware is absent.
"""
